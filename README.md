# Estimating Time Spent on Work Tasks — reproduction code

This repository contains the minimal code and data needed to reproduce the experiments in
**"Estimating Time Spent on Work Tasks"**. It estimates, for nearly every U.S. occupation in
O*NET, the **share of working time spent on each task**, `s(t, o) = w(t, o) · E[f(t, o)]`, by
factoring time into (i) an expected task frequency from O*NET/CPS and (ii) a per-instance
time `w(t, o)` recovered by solving a linear program over LM-elicited pairwise time rankings.

Everything here runs **offline and deterministically**: the LM pairwise judgements are shipped
as a cache under `data/generated_data/`, so re-running the pipeline performs **no API calls**
and reproduces the released estimates exactly.

## Setup

```bash
pip install -r requirements.txt    # Python 3.12; numpy/pandas/scipy/matplotlib/openpyxl
```

All commands below are run **from the repository root**.

## What is reproducible here

| Paper artifact | Command | Expected result |
|---|---|---|
| **Time shares** `s(t,o)` (the core estimates) | `python3 -m find_time_per_task --model_name gpt-5.2 --n_trials 1 --prob_threshold 0.7 --use_cps_constants --find_max_span_weights --start_index 0 --end_index 879` | rewrites `data/generated_data/occupation_time_per_task_*_chosen_max_span_weigths.json`; the per-task time estimates are **byte-identical** to the shipped files (verified) |
| same, **uniform objective** (Eq. 2, App. C) | drop `--find_max_span_weights` | rewrites `..._chosen.json` |
| **Figure 1** (Massage Therapists) | `python3 -m analysis.figure1_massage_therapist_example` | 44% of tasks exposed, 16% of working time exposed |
| **Figure 4** (exposure CCDFs) | `python3 -m analysis.figure4_exposure_share_cdf` | writes the 2 non-band PNGs to `plots/` (see below) |
| **Figure 5** (CCDFs with LP-objective band) | `python3 -m analysis.figure5_exposure_share_cdf_lp_objective_band` | writes the 2 `..._max_span_band` PNGs to `plots/` |
| **Table 2** (human-study agreement) | `python3 validation/user_study_analysis/table2_human_ranking_agreement.py --loop_all_socs` | Kendall τ (LP vs agg.) = 0.66 / 0.58 / 0.61 / 0.57 / 0.50; mean τ (agg. vs indiv.) = 0.50 / 0.67 / 0.65 / 0.56 / 0.46 |
| **Table 3** (top-25 set changes) | `python3 -m analysis.table3_top25_exposure_changes` | 11 of 25 occupations swap out |
| **Time-share correlations** (§Analysis: Tamkin–McCrory time + O*NET descriptors) | `python3 -m analysis.time_share_correlations --use_cps_constants --use_chosen_codes --extra_details gpt-5.2_ONET_30.2_use_cps_constants_prob_thresh=0.7 --use_max_span_time_weights` | importance τ, importance×freq τ, frequency τ, time spearman |
| **Feasible-set size** (§Validation, ℓ2 = 1.50) | `python3 validation/feasible_set_solution_spread.py --use_chosen_codes --extra_details gpt-5.2_ONET_30.2_use_cps_constants_prob_thresh=0.7` | mean biggest ℓ2 difference ≈ 1.50 |
| **Per-task estimate export** (xlsx) | `python3 -m data_formatting.export_per_task_estimates` | rewrites `data/analysis_results/current_per_task_estimates.xlsx` |

`plots/` Figure 4 = `fraction_occupations_at_or_above_fraction_tasks_exposed_alex_vs_hosseini[_moderate_exposure].png`;
Figure 5 = the `..._max_span_band[...]` variants (the shaded band is the Eq. 1 vs Eq. 2 objective).
`_moderate_exposure` is panel (a) (>25% threshold); the plain name is panel (b) (>50%).
(`alex` / `hosseini` in the output filenames denote the simulation-based [Wan et al.] and rubric-based
[Hosseini & Lichtinger] exposure measures.)

The second Table 2 column (mean τ agg. vs. indiv. and frac. significant) is also printed standalone by
`python3 validation/user_study_analysis/mean_kt_aggregate_vs_individual.py`.

### Per-task estimate spreadsheet

`data/analysis_results/current_per_task_estimates.xlsx` (also regenerated by
`data_formatting/export_per_task_estimates.py`) maps every `(occupation, task)` to our **predicted per-instance
time**, the **expected daily frequency**, and the **time spent per day estimate** (their
product, `w(t,o)·E[f(t,o)]`), alongside O*NET importance and the exposure labels.

## Method entry points

- `find_time_per_task.py` — builds and solves the LP per occupation (default objective and
  `--find_max_span_weights`). Uses `--use_cps_constants` and `--prob_threshold 0.7`.
- `time_classification.py` — the LM pairwise time-preference classifier (reads/writes the cache).
- `utils/` — `constants.py`, `compute_onet_hours.py` (CPS-based per-SOC frequency constants),
  `llm_utils.py` (OpenAI Responses API logprob path; only used when recomputing).

## Reproducibility note (please read)

The figures/numbers in the table above are exactly reproducible. Two caveats:

- **Figure 2** is a hand-drawn method diagram and **Figure 3** is a Wall Street Journal
  screenshot — neither is generated by code. (Figure 1's *numbers* are reproduced by
  `analysis/figure1_massage_therapist_example.py`; `paper_reference_figures/massage_exposure.tex`
  is the figure's hand-built table.)
- **Out of scope by design:** the Spearman correlations ρ=0.55 / ρ=0.54 and the full top-25
  lists (Tables 5 & 6) are **not** reproduced here — they come from
  `spearman_predicted_vs_anthropic_times.py` and `compare_top_bottom_occupations_by_weighting.py`
  in the full research repo, which were intentionally excluded from this minimal release.

`paper_reference_figures/` holds the published Figure 4/5 PNGs so you can compare your
regenerated plots against them.

## Re-computing the LM rankings (optional)

Not required for any result above. To regenerate the pairwise judgements from scratch, set
`OPENAI_API_KEY` (model `gpt-5.2`) and delete the relevant
`data/generated_data/*task2time_preference_prob_threshold.json` caches before running
`find_time_per_task.py`.
